import type { TState } from "./types";

const state: TState = {
  workoutStartedType: null,
  isWorkoutStarted: false,
  selectedWorkout: null,
  workoutScheduleList: null,
  userData: null,
};

export default state;
